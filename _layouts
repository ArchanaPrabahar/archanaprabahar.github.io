<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ page.title }} | {{ site.title }}</title>
  <link rel="stylesheet" href="{{ '/assets/css/main.css' | relative_url }}">
</head>
<body>

  <!-- NAVIGATION BAR -->
  <nav style="background-color:#ff6f61; padding:10px; border-radius:10px;">
    <a href="{{ '/' | relative_url }}" style="color:white; margin-right:15px;">Home</a>
    <a href="{{ '/about/' | relative_url }}" style="color:white; margin-right:15px;">About</a>
    <a href="{{ '/cv/' | relative_url }}" style="color:white; margin-right:15px;">CV</a>
    <a href="{{ '/projects/' | relative_url }}" style="color:white;">Projects</a>
  </nav>
  <hr>

  <!-- PAGE CONTENT -->
  <main>
    {{ content }}
  </main>

</body>
</html>

